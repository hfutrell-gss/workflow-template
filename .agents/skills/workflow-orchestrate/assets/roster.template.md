# Resolved roster: __SLUG__

Resolved __DATE__. Authoritative for this session, including cold continuations — a resumed run
reuses these rather than re-deciding. Resolution order and the `prefer:` schema:
`.agents/skills/workflow-orchestrate/references/model-classes.md`.

| tier | lane | dispatch | handle |
|------|------|----------|--------|
| flagship | | `model:` / `agentType:` | |
| workhorse | | | |
| fleet | | | |

## Roles

| role | tier |
|------|------|
| consultant | flagship |
| orchestrator | workhorse |
| worker | fleet |

## Substitutions and unavailable lanes

<!-- Every fallback taken, and why. A degraded lane is a finding, not a detail. If a tier could
     not be filled at all, say so here AND to the user — never substitute a different tier. -->
